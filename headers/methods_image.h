#include <stdio.h>
#include <stdlib.h>
#include <math.h>

unsigned char *img_binarization(unsigned char *, int, int, unsigned char);
unsigned char *img_interpolation(unsigned char *, int, int, int, int);
